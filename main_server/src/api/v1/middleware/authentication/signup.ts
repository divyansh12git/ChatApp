import {User} from "../../interfaces/types"
import { EncrptData,DecryptData,compareHash,generateHash } from "../../helpers";

const SignUp=async(data:User):Promise<Boolean>=>{
    
    return true;
}
export {SignUp}